All libraries are included in the standard python3 package
This script only runs on unix based systems (not win32)

to run the script:

Add the names of the shows you watch to shows.txt
then run:
$ ./dlclean "source directory" "target directory"

example:
./dlclean "prufa" "shows"
